<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ReviewMapper">
	<sql id="cri">
		<if test="type != null">
			<trim prefix=" and (" suffix=")">
				<choose>
					<when test="type == 'TCW'.toString()">
						title like '%'||#{keyword}||'%' or
						content like '%'||#{keyword}||'%' or
						writer like '%'||#{keyword}||'%'
					</when>
					<when test="type == 'TC'.toString()">
						title like '%'||#{keyword}||'%' or
						content like '%'||#{keyword}||'%'
					</when>
					<when test="type == 'TW'.toString()">
						title like '%'||#{keyword}||'%' or
						writer like '%'||#{keyword}||'%'
					</when>
					<when test="type == 'CW'.toString()">
						content like '%'||#{keyword}||'%' or
						writer like '%'||#{keyword}||'%'
					</when>
					<when test="type == 'T'.toString()">
						title like '%'||#{keyword}||'%'
					</when>
					<when test="type == 'C'.toString()">
						content like '%'||#{keyword}||'%'
					</when>
					<when test="type == 'W'.toString()">
						writer like '%'||#{keyword}||'%'
					</when>
				</choose>
			</trim>
		</if>
	</sql>
	<insert id="add" parameterType="rvvo">
		insert into tbl_review
		values(seq_review.nextval, #{title}, #{writer}, #{content}, sysdate,
		sysdate, 0, #{imgfile}, 0)
	</insert>
	<select id="list" resultType="rvvo">
		<![CDATA[
		SELECT rvno, title, writer, content, readcount, modd8, commentcnt
		FROM
		(SELECT /*+INDEX_DESC(tbl_review tbl_reveiw_pk)*/
			rownum rn, rvno, title, writer, content, readcount, modd8, commentcnt 
			FROM TBL_REVIEW 
			WHERE rvno > 0 
			]]>
		<include refid="cri" />
		<![CDATA[		
		and rownum <= #{pageNum}*#{amount})
		WHERE rn > (#{pageNum}-1)*#{amount}
	 	]]>
	</select>
	<select id="detail" parameterType="java.lang.Integer" resultType="rvvo">
		select * from tbl_review where rvno = #{rvno}
	</select>
	<update id="modify" parameterType="rvvo">
		update tbl_review set
		title = #{title}, content = #{content}, modd8 = sysdate, imgfile = #{imgfile}
		where rvno = #{vrno}
	</update>
	<delete id="remove" parameterType="java.lang.Integer">
		delete from tbl_review where rvno = #{rvno}
	</delete>
	<update id="rmImg" parameterType="java.util.HashMap">
		update tbl_review set imgfile = #{imgfile} where rvno = #{rvno}
	</update>
	<select id="total" resultType="java.lang.Integer" parameterType="cri">
		select count(rvno) from tbl_review where rvno > 0
		<include refid="cri" />
	</select>
	<update id="upCount" parameterType="java.util.HashMap">
		update tbl_review set commentcnt = commentcnt + #{sign} where rvno = #{rvno}
	</update>
	<update id="upReadCount" parameterType="java.lang.Integer">
		update tbl_review set readcount = readcount + 1 where rvno = #{rvno}
	</update>
</mapper>
